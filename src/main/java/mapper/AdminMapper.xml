<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.admin.dao.AdminDao" >
  
  <select id="findByAdminnameAndAdminpwd" parameterType="map" resultType="com.example.admin.entity.Admin">
	  select * 
	  from 
	  	t_admin 
	  where 
	  	adminname=#{adminname} and adminpwd=#{adminpwd}
  </select>
</mapper>